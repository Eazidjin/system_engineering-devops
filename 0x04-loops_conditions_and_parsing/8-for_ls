#!/usr/bin/env bash
# This script lists non-hidden files/directories and prints part after the first dash

for file in *; do
  # Check if the filename contains a dash
  if [[ "$file" == *-* ]]; then
    # Print the part after the first dash
    echo "${file#*-}"
  fi
done
